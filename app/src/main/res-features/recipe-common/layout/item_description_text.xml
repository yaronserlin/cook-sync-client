<?xml version="1.0" encoding="utf-8"?>
<!-- A single paragraph block within the recipe description, interspersed with
     item_description_image.xml blocks per the design's descBlocks pattern. -->
<TextView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/description_text"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginTop="@dimen/dp_10"
    android:alpha="0.8"
    android:lineSpacingMultiplier="1.4"
    android:textColor="@color/color_text"
    android:textSize="@dimen/sp_14_5"
    tools:text="The version I make when there is nothing in the fridge except eggs and a tin of tomatoes." />
