<?xml version="1.0" encoding="utf-8"?>
<!-- Compact search-result row, matching the design's search screen (distinct from the
     elevated item_recipe_card used on Home): 76dp flat thumbnail, title/author/rating/time,
     trailing chevron, bottom divider — no card background or elevation. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="?attr/selectableItemBackground"
    android:gravity="center_vertical"
    android:orientation="horizontal"
    android:paddingHorizontal="16dp"
    android:paddingVertical="12dp">

    <com.google.android.material.card.MaterialCardView
        android:layout_width="76dp"
        android:layout_height="76dp"
        app:cardCornerRadius="16dp"
        app:cardElevation="0dp"
        app:strokeWidth="0dp">

        <ImageView
            android:id="@+id/recipe_image"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scaleType="centerCrop"
            tools:src="@tools:sample/backgrounds/scenic" />

    </com.google.android.material.card.MaterialCardView>

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="12dp"
        android:layout_weight="1"
        android:orientation="vertical">

        <TextView
            android:id="@+id/recipe_title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:fontFamily="@font/caprasimo"
            android:maxLines="1"
            android:textColor="@color/color_text"
            android:textSize="17sp"
            tools:text="Shakshuka with feta" />

        <TextView
            android:id="@+id/recipe_subtitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="3dp"
            android:alpha="0.65"
            android:ellipsize="end"
            android:maxLines="1"
            android:textColor="@color/color_text"
            android:textSize="12sp"
            tools:text="Yaron Serlin · 6 reviews" />

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="6dp"
            android:gravity="center_vertical"
            android:orientation="horizontal">

            <ImageView
                android:layout_width="12dp"
                android:layout_height="12dp"
                android:src="@drawable/ic_star"
                app:tint="@color/color_accent_700" />

            <TextView
                android:id="@+id/rating_text"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="4dp"
                android:layout_marginEnd="10dp"
                android:textColor="@color/color_accent_700"
                android:textSize="12sp"
                android:textStyle="bold"
                tools:text="3.8" />

            <TextView
                android:id="@+id/time_text"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:alpha="0.65"
                android:textColor="@color/color_text"
                android:textSize="12sp"
                android:textStyle="bold"
                tools:text="25 mins" />

        </LinearLayout>

    </LinearLayout>

    <ImageView
        android:layout_width="18dp"
        android:layout_height="18dp"
        android:layout_marginStart="8dp"
        android:alpha="0.4"
        android:contentDescription="@null"
        android:src="@drawable/ic_arrow_forward" />

</LinearLayout>
