<?xml version="1.0" encoding="utf-8"?>
<!-- Shared horizontal recipe row, used by both "My Recipes" and "Favorites" (which use
     identical card styling in the design, differing only in the trailing action: an overflow
     menu for My Recipes, a heart toggle for Favorites) and a bottom note strip when the user
     has a private note on that recipe, matching the design's favorites-list note callout. -->
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="@dimen/dp_16"
    android:layout_marginBottom="@dimen/dp_12"
    app:cardBackgroundColor="@color/color_surface"
    app:cardCornerRadius="@dimen/dp_20"
    app:cardElevation="@dimen/dp_0"
    app:strokeWidth="@dimen/dp_0">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center_vertical"
            android:orientation="horizontal"
            android:padding="@dimen/dp_12">

            <com.google.android.material.card.MaterialCardView
                android:layout_width="@dimen/dp_72"
                android:layout_height="@dimen/dp_72"
                app:cardCornerRadius="@dimen/dp_14"
                app:cardElevation="@dimen/dp_0"
                app:strokeWidth="@dimen/dp_0">

                <ImageView
                    android:id="@+id/recipe_image"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:scaleType="centerCrop"
                    tools:src="@tools:sample/backgrounds/scenic" />

            </com.google.android.material.card.MaterialCardView>

            <LinearLayout
                android:layout_width="@dimen/dp_0"
                android:layout_height="wrap_content"
                android:layout_marginStart="@dimen/dp_12"
                android:layout_weight="1"
                android:orientation="vertical">

                <TextView
                    android:id="@+id/recipe_title"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:ellipsize="end"
                    android:fontFamily="@font/caprasimo"
                    android:maxLines="1"
                    android:textColor="@color/color_text"
                    android:textSize="@dimen/sp_17"
                    tools:text="Shakshuka with feta" />

                <TextView
                    android:id="@+id/recipe_subtitle"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="@dimen/dp_2"
                    android:alpha="0.65"
                    android:ellipsize="end"
                    android:maxLines="1"
                    android:textColor="@color/color_text"
                    android:textSize="@dimen/sp_12"
                    tools:text="6 reviews" />

                <LinearLayout
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="@dimen/dp_6"
                    android:gravity="center_vertical"
                    android:orientation="horizontal">

                    <ImageView
                        android:layout_width="@dimen/dp_13"
                        android:layout_height="@dimen/dp_13"
                        android:src="@drawable/ic_star"
                        app:tint="@color/color_accent_700" />

                    <TextView
                        android:id="@+id/rating_text"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:layout_marginStart="@dimen/dp_4"
                        android:layout_marginEnd="@dimen/dp_10"
                        android:textColor="@color/color_accent_700"
                        android:textSize="@dimen/sp_12"
                        android:textStyle="bold"
                        tools:text="3.8" />

                    <TextView
                        android:id="@+id/visibility_badge"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:background="@drawable/bg_tag_public"
                        android:paddingHorizontal="@dimen/dp_9"
                        android:paddingVertical="@dimen/dp_3"
                        android:textColor="@color/color_accent_2_800"
                        android:textSize="@dimen/sp_10_5"
                        android:textStyle="bold"
                        android:visibility="gone"
                        tools:text="Public"
                        tools:visibility="visible" />

                </LinearLayout>

            </LinearLayout>

            <ImageButton
                android:id="@+id/btn_trailing_action"
                android:layout_width="@dimen/dp_34"
                android:layout_height="@dimen/dp_34"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:contentDescription="@string/recipe_options_description"
                android:scaleType="centerInside"
                android:src="@drawable/ic_more_horiz"
                app:tint="@color/color_text" />

        </LinearLayout>

        <!-- Private note strip, matching the design's favorites-list note callout; hidden
             when the recipe has no personal note. -->
        <TextView
            android:id="@+id/recipe_note_strip"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@drawable/bg_note_strip"
            android:drawablePadding="@dimen/dp_7"
            android:lineSpacingMultiplier="1.35"
            android:padding="@dimen/dp_12"
            android:textColor="@color/color_accent_2_800"
            android:textSize="@dimen/sp_12_5"
            android:visibility="gone"
            app:drawableStartCompat="@drawable/ic_edit"
            app:drawableTint="@color/color_accent_2_800"
            tools:text="Private note — step 3 only needs 10 minutes on my stove."
            tools:visibility="visible" />

    </LinearLayout>

</com.google.android.material.card.MaterialCardView>
