<?xml version="1.0" encoding="utf-8"?>
<!-- One row in the tag-autocomplete dropdown popup, matching the design's tag-suggestion row
     (hash/tag icon + name, accent-tinted on the top match). -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="?attr/selectableItemBackground"
    android:gravity="center_vertical"
    android:minHeight="@dimen/dp_46"
    android:orientation="horizontal"
    android:paddingHorizontal="@dimen/dp_14"
    android:paddingVertical="@dimen/dp_10">

    <ImageView
        android:id="@+id/iv_tag_suggestion_icon"
        android:layout_width="@dimen/dp_14"
        android:layout_height="@dimen/dp_14"
        android:src="@drawable/ic_tag"
        app:tint="@color/color_accent_700" />

    <TextView
        android:id="@+id/tv_tag_suggestion_name"
        android:layout_width="@dimen/dp_0"
        android:layout_height="wrap_content"
        android:layout_marginStart="@dimen/dp_9"
        android:layout_weight="1"
        android:ellipsize="end"
        android:maxLines="1"
        android:textColor="@color/color_text"
        android:textSize="@dimen/sp_13_5" />

</LinearLayout>
