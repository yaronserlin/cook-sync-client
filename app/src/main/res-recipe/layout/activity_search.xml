<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/color_bg"
    android:fitsSystemWindows="true"
    android:orientation="vertical"
    tools:context=".ui.recipe.SearchActivity">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:paddingHorizontal="8dp"
        android:paddingTop="12dp">

        <ImageButton
            android:id="@+id/btn_back"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/action_back"
            android:padding="8dp"
            android:src="@drawable/ic_arrow_back"
            app:tint="@color/color_text" />

        <androidx.appcompat.widget.SearchView
            android:id="@+id/search_view"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="4dp"
            android:layout_marginEnd="4dp"
            android:background="@drawable/bg_input"
            app:iconifiedByDefault="false"
            app:queryBackground="@android:color/transparent"
            app:queryHint="@string/search_hint" />

        <FrameLayout
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:layout_marginStart="4dp">

            <ImageButton
                android:id="@+id/btn_filters"
                android:layout_width="40dp"
                android:layout_height="40dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:contentDescription="@string/nav_search"
                android:padding="9dp"
                android:src="@drawable/ic_filter"
                app:tint="@color/color_text" />

            <TextView
                android:id="@+id/tv_filters_badge"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="top|end"
                android:background="@drawable/bg_accent_circle"
                android:gravity="center"
                android:includeFontPadding="false"
                android:minWidth="16dp"
                android:minHeight="16dp"
                android:paddingHorizontal="3dp"
                android:textColor="@color/color_bg"
                android:textSize="9.5sp"
                android:textStyle="bold"
                android:visibility="gone"
                tools:text="2"
                tools:visibility="visible" />

        </FrameLayout>

    </LinearLayout>

    <!-- "Matching tags" suggestions, shown only while the query is non-empty and at least one
         known tag name contains it. -->
    <LinearLayout
        android:id="@+id/matching_tags_section"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingHorizontal="16dp"
        android:paddingTop="12dp"
        android:visibility="gone"
        tools:visibility="visible">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginBottom="8dp"
            android:letterSpacing="0.08"
            android:text="@string/search_matching_tags_label"
            android:textAllCaps="true"
            android:textColor="@color/color_accent_2_700"
            android:textSize="11sp"
            android:textStyle="bold" />

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rv_matching_tags"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:clipToPadding="false"
            android:orientation="horizontal"
            app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
            tools:listitem="@layout/item_tag_chip" />

    </LinearLayout>

    <!-- Active-results summary, matching the design's "N recipes · sorted by X" label —
         this is the screen it was designed for; Home no longer shows it. -->
    <TextView
        android:id="@+id/tv_results_summary"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:paddingHorizontal="16dp"
        android:alpha="0.6"
        android:textColor="@color/color_text"
        android:textSize="12.5sp"
        android:textStyle="bold"
        android:visibility="gone"
        tools:text="3 recipes · sorted by rating"
        tools:visibility="visible" />

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rv_results"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:clipToPadding="false"
            android:paddingTop="4dp"
            android:paddingBottom="24dp"
            app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
            tools:listitem="@layout/item_search_result_row" />

        <TextView
            android:id="@+id/tv_empty_state"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:layout_marginHorizontal="32dp"
            android:gravity="center"
            android:text="@string/search_empty_prompt"
            android:textColor="@color/color_text"
            android:alpha="0.5"
            android:textSize="14sp" />

        <!-- No-results-with-active-filters state, matching the design's dedicated "noresults"
             screen: names the conflicting filter and offers to drop it, rather than a shrug. -->
        <LinearLayout
            android:id="@+id/no_results_filtered_state"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:orientation="vertical"
            android:gravity="center"
            android:paddingHorizontal="32dp"
            android:visibility="gone"
            tools:visibility="visible">

            <FrameLayout
                android:layout_width="88dp"
                android:layout_height="88dp"
                android:background="@drawable/bg_accent2_circle_100">

                <ImageView
                    android:layout_width="36dp"
                    android:layout_height="36dp"
                    android:layout_gravity="center"
                    android:contentDescription="@null"
                    android:src="@drawable/ic_search"
                    app:tint="@color/color_accent_2_700" />

            </FrameLayout>

            <TextView
                android:id="@+id/tv_no_results_title"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="18dp"
                android:fontFamily="@font/caprasimo"
                android:textColor="@color/color_text"
                android:textSize="22sp"
                tools:text="@string/search_no_results_title" />

            <TextView
                android:id="@+id/tv_no_results_subtitle"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="6dp"
                android:alpha="0.72"
                android:gravity="center"
                android:textColor="@color/color_text"
                android:textSize="14sp"
                tools:text="@string/search_no_results_subtitle" />

            <com.google.android.material.button.MaterialButton
                android:id="@+id/btn_remove_filter"
                style="@style/CookSync.Button.Primary"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="20dp"
                android:minWidth="0dp"
                android:paddingHorizontal="20dp"
                tools:text="Remove &quot;Vegan&quot;" />

            <com.google.android.material.button.MaterialButton
                android:id="@+id/btn_clear_all_filters"
                style="@style/CookSync.Button.Secondary"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="10dp"
                android:minWidth="0dp"
                android:paddingHorizontal="20dp"
                android:text="@string/search_action_clear_all_filters" />

            <com.google.android.material.button.MaterialButton
                android:id="@+id/btn_clear_search"
                style="@style/CookSync.Button.Primary"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="20dp"
                android:minWidth="0dp"
                android:paddingHorizontal="20dp"
                android:text="@string/search_action_clear_search"
                android:visibility="gone" />

        </LinearLayout>

        <ProgressBar
            android:id="@+id/progress"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:layout_gravity="center"
            android:indeterminateTint="@color/color_accent"
            android:visibility="gone" />

    </FrameLayout>

</LinearLayout>
